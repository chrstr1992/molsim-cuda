name: CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
       #- uses: actions/molsim-cuda@master
    - name: Generate test-file
    #run: grep "Auto" test.txt
      run: pwd
      shell: bash
    - name: Run a multi-line script
      run: |
        echo Add other actions to build,
        echo test, and deploy your project.
